/**
 * OfficerService - Placeholder API service for Officer module (Member 2)
 */

export const officerService = {
  submitQualityCheck: async (payload) => {
    console.log('[Placeholder API] officerService.submitQualityCheck called:', payload);
    return { success: true };
  },
  submitWeighment: async (payload) => {
    console.log('[Placeholder API] officerService.submitWeighment called:', payload);
    return { success: true };
  },
};
